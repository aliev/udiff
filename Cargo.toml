[workspace]
members = ["diffwatch", "loopdiff"]
resolver = "2"

[workspace.package]
edition = "2024"
rust-version = "1.88"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
panic = "abort"

[profile.dist]
inherits = "release"
lto = "thin"
